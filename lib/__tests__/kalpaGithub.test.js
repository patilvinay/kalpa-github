const assert = require('assert');
const kalpaGithub = require('../index.js');

describe('kalpaGithub', () => {
  it('has a test', () => {
    assert(false, 'kalpaGithub should have a test');
  });
});
